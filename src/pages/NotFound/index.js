import { Box, Typography } from '@mui/material';

export default function NotFoundPage() {
  return (
    <Box>
      <Typography variant="h2">This is the not found page</Typography>
    </Box>
  );
}
