export default function Modal() {
  return {
    styleOverrides: {
      root: {
        display: 'inline-flex',
        justifyContent: 'center',
        alignItems: 'center',
      },
    },
  };
}
