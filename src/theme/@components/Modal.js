export default function Modal() {
  return {
    styleOverrides: {
      root: {
        display: 'inline-flex',
        justifyContent: 'center',
        alignItems: 'center',
      },
      // backdrop: {
      //   background: 'rgba(0, 0, 0, 0.6)',
      // },
    },
  };
}
